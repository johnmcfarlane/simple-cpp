// built with Clang; optimized for fuzz testing
{
    "name": "Clang fuzzer",
    "build": {
        "dockerfile": "${localWorkspaceFolder}/.devcontainer/Dockerfile",
        "args": {
            "base_image": "base-clang",
            "base_image_version": "2026-04-03",
            "profile": "clang-test-fuzzer"
        },
        "context": "${localWorkspaceFolder}",
        "options": ["--progress=plain"]
    },
    "remoteUser": "tux",
    "postCreateCommand": {
        "configure build system": "cmake --toolchain $BUILD_DIR/conan_toolchain.cmake -B $BUILD_DIR -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH=$BUILD_DIR -DENABLE_FUZZER=1 -G Ninja -S ${localEnv:PROJECT_SUBDIRECTORY:.}"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cmake-tools",
                "ms-vscode.cpptools"
            ]
        }
    }
}
