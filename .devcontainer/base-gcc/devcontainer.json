// image with GCC trunk build
{
    "name": "Build GCC image",
    "build": {
        "dockerfile": "${localWorkspaceFolder}/.devcontainer/base-gcc/Dockerfile",
        "args": {
            "image_version": "8db7ba7ffbf"
        },
        "context": "${localWorkspaceFolder}",
        "options": ["--progress=plain"]
    }
}
